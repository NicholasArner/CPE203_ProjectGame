import processing.core.PImage;

import java.util.List;

public class EndGoal extends Entity{
    protected EndGoal(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
